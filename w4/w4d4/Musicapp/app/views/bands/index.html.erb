<!--list of all bands-->
<h1>List of Artists</h1>

<section class="band-index">
  <ul>
    <% @bands.sort.reverse.each do |band| %>
          <li><%= link_to "#{band.name}", band_url(band) %></li>
    <% end %>
  </ul>
</section>

<%= link_to "Add an Artist", new_band_url%>
